# Copy to .dev.vars for local development. Real values via `wrangler secret put` for production.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
RESEND_API_KEY=re_xxxxxxxx
SESSION_SECRET=replace-with-32-bytes-random-base64
